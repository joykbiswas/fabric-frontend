import React from 'react';

const Products = () => {
    return (
        <div className="bg-gradient-to-t from-white via-white h-screen">
            All Products
        </div>
    );
};

export default Products;